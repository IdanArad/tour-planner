---
paths:
  - "components/**/*.tsx"
---

- Tailwind utility classes directly on elements — no CSS modules or styled-components
- shadcn/ui components for cards, tables, badges, tabs (in `components/ui/`)
- Dark mode via CSS custom properties (globals.css)
- Color-coded status badges across shows and reachouts
- shadcn/ui uses `@base-ui/react` (NOT Radix UI) — use `render` prop instead of `asChild`
